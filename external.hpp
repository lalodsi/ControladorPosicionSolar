/** CLASE NO UTILIZADA
 * @brief Clase abstracta external que define atributos principales para todos los elementos externos
 */
class external
{
protected:
    int pins;

// public:
    // virtual void init();
};
